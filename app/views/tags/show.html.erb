<div class="buffer-both">
  <div class="questions-list">
      <h3 class="title-medium text-center">Вопросы с хэштегом: #<%= @tag.name %></h3>
    <% @questions.uniq.each do |question| %>
      <%= render partial: 'questions/question', object: question %>
    <% end %>
  </div>
</div>
