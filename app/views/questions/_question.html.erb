<div class="question">
  <div class="question-content">
    <%# Текст вопроса %>
    <div class="question-text text-lead">
      <%= question.text %>
    </div>

    <div class="buffer-bottom">
      <% question_tags(question.text).map do |tag| %>
      <%= link_to tag, tag_path(tag.delete('#'))%>
      <% end %>
    </div>

    <%# Автор вопроса %>
    <div class="buffer-bottom">
      <% if question.author %>
        <%= link_to "Спрашивает @#{question.author.username}", user_path(question.author)%>
      <% end %>
    </div>

    <%# Текст ответа, если он есть %>
    <% if question.answer.present? && current_user %>
      <div class="question-answer text-lead">
        <%= question.answer %>
      </div>
      <div>
        <% question_tags(question.answer).map do |tag| %>
          <%= link_to tag, tag_path(tag.delete('#'))%>
        <% end %>
      </div>
    <% end %>

    <%# Дата создания вопроса (отформатированная) %>
    <div class="question-date">
      <%= l(question.created_at, format: :short) %>
    </div>
  </div>

  <% if question.user == current_user %>
    <% edit_icon = question.answer.blank? ? 'reply' : 'pencil' %>

    <div class="question-actions">
      <%= link_to fa_icon(edit_icon), edit_question_path(question) %>
      <%= link_to fa_icon('trash-o'), question_path(question),
                  method: :delete, data: {confirm: 'Точно удалить вопрос?'} %>
    </div>
  <% end %>
</div>
